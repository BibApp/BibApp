%fieldset
  %legend="Group"
  - form_for :group, Group.new, :url => {:action => :create_group }, :method => :post do |form|
    = text_field_with_auto_complete :group, :name, {}, {:method => :get, :skip_style => true, :frequency => 0.5}
    = submit_tag "Add"