%div{:class => "span-24"}
  %div{:class => "span-22 prepend-1"}
    %h2="Groups (#{@page}) - #{link_to "Add", new_group_path}"
    
    = render :partial => "shared/alpha_pagination"

    %ol.groups
      -@current_objects.each do |group|
        %li.group
          =link_to group.name, group
