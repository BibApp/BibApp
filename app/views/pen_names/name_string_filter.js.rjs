if @results.size < 1
  page.replace_html 'inactive', t('.no_results', :phrase => @phrase)
else
  page.replace_html 'inactive', :partial => "name_string", :collection => @results, :locals => {:person => @person, :selected => false}
end
