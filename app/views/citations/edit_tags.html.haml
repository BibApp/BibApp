- @citation = Citation.find(params[:id])

- form_for :citation, @citation, :url => { :action => "update_tags", :id => @citation.id }, :html => {:method => :put} do |form|
  %p
    = render :partial => 'tags'
    
  
  %p.submit
    = submit_tag :save