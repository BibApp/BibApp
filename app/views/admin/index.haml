/ === Admin === Index ===
/ Main
%div{:class => "span-24"}
  %div{:class => "span-22 prepend-1"}
    = render :partial => "shared/flash_messages"
      
    %h2 Administrative Tasks:
    = render :partial => "admin_nav"

    - if @tab_name == "works"
      %ul#tasks
        %li=link_to "Add works", new_work_path
        %li=link_to "Manage unverified contributorships", admin_contributorships_path
        //%li=link_to "Edit Works", edit_work_path
        %li=link_to "View duplicate works", :controller=>"admin", :action =>"duplicates"
        %li=link_to "Prepare files for archiving", :action => "ready_to_archive"

    - if @tab_name == "groups"
      %ul#tasks
        %li=link_to "Add a group", new_group_path

    - if @tab_name == "people"
      %ul#tasks
        %li=link_to "Add a person", new_person_path

    - if @tab_name == "publications"
      %ul#tasks
        %li=link_to "Add a publication", new_publication_path
        %li=link_to "Update publication authorities", authorities_publications_path

    - if @tab_name == "publishers"
      %ul#tasks
        %li=link_to "Add a publisher", new_publication_path
        %li=link_to "Update publisher authorities", authorities_publishers_path

    - if @tab_name == "roles"
      %ul#tasks
        %li=link_to "Update system roles", :controller=>"roles", :action =>"index"
          
