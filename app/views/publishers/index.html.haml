- include_stylesheet('common/sherpa')

- if current_user_role?("editor", Group)
  = render :layout => 'layouts/admin_bar' do
    %li #{link_to "Add a Publisher", new_publisher_path}
    %li #{link_to "Admin Publisher Authorities", authorities_publishers_path(:page => params[:page])}

= render :layout => '/layouts/main_body', :locals => {:header => 'Publishers'} do
  = render "shared/alpha_pagination"

  = render 'index_table'

  %br/
  %br/
  = render "shared/alpha_pagination"

