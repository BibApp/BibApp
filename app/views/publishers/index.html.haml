%div{:class => "column span-24"}
  %div{:class => "column span-22 prepend-1"}
    %h2="Publishers (#{@page}) - #{link_to "Add", new_publisher_path}"

    = render :partial => "shared/alpha_pagination"
    
    %ol.publishers
      -@current_objects.each do |publisher|
        %li
          =link_to "#{publisher.name}", publisher_path(publisher)
    
  