<h1><%= t('.header') %></h1>

<table>
  <tr>
  </tr>

<% for keywording in @keywordings %>
  <tr>
    <td><%= link_to t('app.show'), keywording %></td>
    <td><%= link_to t('app.edit'), edit_keywording_path(keywording) %></td>
    <td><%= link_to t('app.destroy'), keywording, :confirm => t('app.are_you_sure'), :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to t('.new'), new_keywording_path %>
