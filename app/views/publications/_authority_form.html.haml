= render 'shared/pub_common/authority_form', :authorities => authorities,
  :submit_url => update_multiple_publications_path, :page => page,
  :headers => [Publication.human_attribute_name(:issn_isbn), t('common.publications.publication_name'), Publisher.human_name, Work.human_name_pl]
