%ul#edit-actions
  %li=link_to "Edit Info", edit_person_path(@person)
  %li=link_to "Manage Roles", person_roles_path(@person)
  %li=link_to "Manage Groups", {:controller => "memberships", :action => "new", :params => {"person_id", @person.id}}
  %li=link_to "Pen Names", {:controller => "pen_names", :action => "new", :params => {"person_id", @person.id}}
  %li=link_to "Contributorships", {:controller => "contributorships", :action => "index", :params => {"person_id", @person.id}}
  %li=link_to "Add Works", new_person_work_path(@person)
  %li=link_to "Archive Analysis", archivable_contributorships_path(:person_id => @person.id)
