- c = params[:controller]
%ul#local-pages
  %li{give_current_class_if_equal('groups', c)}= link_to t('app.home'), group_path(@group)
  %li{give_current_class_if_equal('works', c)}= link_to Work.human_name_pl, group_works_path(@group)
  %li{give_current_class_if_equal('people', c)}= link_to Person.human_name_pl, group_people_path(@group)
