/ === Sessions === Cart ===
/ Main
%div{:class => "span-24"}
  %div{:class => "span-22 prepend-1"}
    - if @works.nil? || @works.empty?
      %h2 Cart
      %p Nothing added yet.
    - else
      %ul#edit-actions
        %li=link_to_deletecart
      
      %h2 Cart
      - @works.each do |work|
        = render :partial => "shared/work", :locals => {:work => work}