-# requires form and field list
%fieldset
  %legend #{@work.class.human_name}

  = hidden_field_tag :klass, @work.class.to_s
  = render 'works/forms/fields/form_buttons', :form => form

  %ul
    - field_list.each do |field|
      %li= render "works/forms/fields/#{field}", :form => form
