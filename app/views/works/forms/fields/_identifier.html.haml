-# Identifier field
-#
-# Possible Input Variables:
-#    :form     => current form (REQUIRED)
-#    :label    => Label to display (default: Identifier)

-# Initialize optional input variables
- label ||= nil
= render 'works/forms/fields/generic_text_field', :form => form, :attribute => :identifier, :label => label
