-# Publisher text field, with auto-complete
%p
  %label Publisher:
  - @publisher = @work.publisher unless @work.publisher.nil?
  = text_field_with_auto_complete :publisher, :name, {:class=>'text', :size => 40}, {:method => :get, :skip_style => true, :frequency => 0.5} 
