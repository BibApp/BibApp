-# Discipline field
-#
-# Possible Input Variables:
-#    :form     => current form (REQUIRED)
-#    :label    => Label to display (default: Discipline)

-# Initialize optional input variables
- label ||= "Discipline"

= form.label :discipline, "#{label}:"
= form.text_field :discipline, :class=>'text'