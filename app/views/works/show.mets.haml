-# METS template is actually in the "_package.mets.haml" partial template
-# (this allows us to load that partial template elsewhere)
= render :partial => 'package', :locals => {:work => @work, :filenames_only => @filenames_only}