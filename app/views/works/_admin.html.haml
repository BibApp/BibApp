- if Sword2Client.configured? and @work.ready_to_archive?
  %fieldset
    %legend #{t 'works.layouts.merge_form.accepted'}
    %ul#tasks
      %li
        = link_to t('works.admin.link'), admin_deposit_via_sword_url(:work_id => @work)