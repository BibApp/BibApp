-#If SWORD is configured, and Work is "Ready for Archiving"
-if SwordClient.configured? and WorkArchiveState.ready_to_archive?(@work)
  %fieldset
    %legend Administrative Tasks
    %ul#tasks
      %li
        = link_to "Deposit Immediately via SWORD", :controller => "admin", :action => "deposit_via_sword", :work_id => @work    